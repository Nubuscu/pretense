// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"nubuscu/pretense/ent/topic"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
)

// Topic is the model entity for the Topic schema.
type Topic struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// MetaLabels holds the value of the "meta_labels" field.
	MetaLabels []string `json:"meta_labels,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TopicQuery when eager-loading is set.
	Edges TopicEdges `json:"edges"`
}

// TopicEdges holds the relations/edges for other nodes in the graph.
type TopicEdges struct {
	// ReviewedBy holds the value of the reviewed_by edge.
	ReviewedBy []*Review `json:"reviewed_by,omitempty"`
	// Includes holds the value of the includes edge.
	Includes []*Album `json:"includes,omitempty"`
	// TaggedWith holds the value of the tagged_with edge.
	TaggedWith []*Tag `json:"tagged_with,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
	// totalCount holds the count of the edges above.
	totalCount [3]map[string]int

	namedReviewedBy map[string][]*Review
	namedIncludes   map[string][]*Album
	namedTaggedWith map[string][]*Tag
}

// ReviewedByOrErr returns the ReviewedBy value or an error if the edge
// was not loaded in eager-loading.
func (e TopicEdges) ReviewedByOrErr() ([]*Review, error) {
	if e.loadedTypes[0] {
		return e.ReviewedBy, nil
	}
	return nil, &NotLoadedError{edge: "reviewed_by"}
}

// IncludesOrErr returns the Includes value or an error if the edge
// was not loaded in eager-loading.
func (e TopicEdges) IncludesOrErr() ([]*Album, error) {
	if e.loadedTypes[1] {
		return e.Includes, nil
	}
	return nil, &NotLoadedError{edge: "includes"}
}

// TaggedWithOrErr returns the TaggedWith value or an error if the edge
// was not loaded in eager-loading.
func (e TopicEdges) TaggedWithOrErr() ([]*Tag, error) {
	if e.loadedTypes[2] {
		return e.TaggedWith, nil
	}
	return nil, &NotLoadedError{edge: "tagged_with"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Topic) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case topic.FieldMetaLabels:
			values[i] = new([]byte)
		case topic.FieldID:
			values[i] = new(sql.NullInt64)
		case topic.FieldName:
			values[i] = new(sql.NullString)
		case topic.FieldCreatedAt, topic.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type Topic", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Topic fields.
func (t *Topic) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case topic.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			t.ID = int(value.Int64)
		case topic.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				t.CreatedAt = value.Time
			}
		case topic.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				t.UpdatedAt = value.Time
			}
		case topic.FieldMetaLabels:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field meta_labels", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &t.MetaLabels); err != nil {
					return fmt.Errorf("unmarshal field meta_labels: %w", err)
				}
			}
		case topic.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				t.Name = value.String
			}
		}
	}
	return nil
}

// QueryReviewedBy queries the "reviewed_by" edge of the Topic entity.
func (t *Topic) QueryReviewedBy() *ReviewQuery {
	return (&TopicClient{config: t.config}).QueryReviewedBy(t)
}

// QueryIncludes queries the "includes" edge of the Topic entity.
func (t *Topic) QueryIncludes() *AlbumQuery {
	return (&TopicClient{config: t.config}).QueryIncludes(t)
}

// QueryTaggedWith queries the "tagged_with" edge of the Topic entity.
func (t *Topic) QueryTaggedWith() *TagQuery {
	return (&TopicClient{config: t.config}).QueryTaggedWith(t)
}

// Update returns a builder for updating this Topic.
// Note that you need to call Topic.Unwrap() before calling this method if this Topic
// was returned from a transaction, and the transaction was committed or rolled back.
func (t *Topic) Update() *TopicUpdateOne {
	return (&TopicClient{config: t.config}).UpdateOne(t)
}

// Unwrap unwraps the Topic entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (t *Topic) Unwrap() *Topic {
	_tx, ok := t.config.driver.(*txDriver)
	if !ok {
		panic("ent: Topic is not a transactional entity")
	}
	t.config.driver = _tx.drv
	return t
}

// String implements the fmt.Stringer.
func (t *Topic) String() string {
	var builder strings.Builder
	builder.WriteString("Topic(")
	builder.WriteString(fmt.Sprintf("id=%v, ", t.ID))
	builder.WriteString("created_at=")
	builder.WriteString(t.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(t.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("meta_labels=")
	builder.WriteString(fmt.Sprintf("%v", t.MetaLabels))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(t.Name)
	builder.WriteByte(')')
	return builder.String()
}

// NamedReviewedBy returns the ReviewedBy named value or an error if the edge was not
// loaded in eager-loading with this name.
func (t *Topic) NamedReviewedBy(name string) ([]*Review, error) {
	if t.Edges.namedReviewedBy == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := t.Edges.namedReviewedBy[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (t *Topic) appendNamedReviewedBy(name string, edges ...*Review) {
	if t.Edges.namedReviewedBy == nil {
		t.Edges.namedReviewedBy = make(map[string][]*Review)
	}
	if len(edges) == 0 {
		t.Edges.namedReviewedBy[name] = []*Review{}
	} else {
		t.Edges.namedReviewedBy[name] = append(t.Edges.namedReviewedBy[name], edges...)
	}
}

// NamedIncludes returns the Includes named value or an error if the edge was not
// loaded in eager-loading with this name.
func (t *Topic) NamedIncludes(name string) ([]*Album, error) {
	if t.Edges.namedIncludes == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := t.Edges.namedIncludes[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (t *Topic) appendNamedIncludes(name string, edges ...*Album) {
	if t.Edges.namedIncludes == nil {
		t.Edges.namedIncludes = make(map[string][]*Album)
	}
	if len(edges) == 0 {
		t.Edges.namedIncludes[name] = []*Album{}
	} else {
		t.Edges.namedIncludes[name] = append(t.Edges.namedIncludes[name], edges...)
	}
}

// NamedTaggedWith returns the TaggedWith named value or an error if the edge was not
// loaded in eager-loading with this name.
func (t *Topic) NamedTaggedWith(name string) ([]*Tag, error) {
	if t.Edges.namedTaggedWith == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := t.Edges.namedTaggedWith[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (t *Topic) appendNamedTaggedWith(name string, edges ...*Tag) {
	if t.Edges.namedTaggedWith == nil {
		t.Edges.namedTaggedWith = make(map[string][]*Tag)
	}
	if len(edges) == 0 {
		t.Edges.namedTaggedWith[name] = []*Tag{}
	} else {
		t.Edges.namedTaggedWith[name] = append(t.Edges.namedTaggedWith[name], edges...)
	}
}

// Topics is a parsable slice of Topic.
type Topics []*Topic

func (t Topics) config(cfg config) {
	for _i := range t {
		t[_i].config = cfg
	}
}
